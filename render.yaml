services:
  - type: web
    name: minefield-server
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    ports:
      - 10000
    healthCheckPath: /
    envVars:
      - key: Jwt__Key
        value: your-very-secret-jwt-key